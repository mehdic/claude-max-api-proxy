# claude-proxy v1.0.7

Public-readiness release on top of v1.0.6.

## Highlights

- Sanitized public documentation to remove local operator details and project-specific notes.
- Replaced machine-specific `n8n-mcp` default path with a portable `PATH` lookup.
- Kept the OpenClaw integration instructions while avoiding private alerting examples.
- Added dedicated public setup, configuration, and OpenClaw integration guides.
- Added a prebuilt npm release asset (`claude-proxy-1.0.7.tgz`) so users can install without building from source.
- Verified build and full test suite after the cleanup.

## Validation

- `npm run build`
- `npm test` → 265 passing
